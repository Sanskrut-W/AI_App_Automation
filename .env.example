# Environment: development | test | production
NODE_ENV=development

# Overrides config/*.json "logging.level" (dot-path -> UPPER_SNAKE_CASE)
LOGGING_LEVEL=info

# Optional — without it, AI screen analysis and test case generation are skipped (crawl + report
# still run). Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=AQ.Ab8RN6KMp_RWAAXJ7R4LS9Rxm_iBPGSuEwFEPKgM3RW_xuwsRw

# Overrides config/default.json "appium.*" — where the Appium server is listening.
# APPIUM_HOSTNAME=localhost
# APPIUM_PORT=4723
# APPIUM_PATH=/
# APPIUM_PROTOCOL=http
